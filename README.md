# S3 POC


# Installation

## Native
```bash
npm ci && npm run bvuild
composer install
php artisan serve
```


## Docker
```bash
docker compose run --rm composer
docker compose run --rm npm
docker compose up php
```
